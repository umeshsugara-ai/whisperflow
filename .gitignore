# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
.venv-build/
venv/
env/

# Installer build artifacts (scripts/build_installer.ps1)
build/
dist/
installer/Output/

# Runtime logs
logs/
*.log

# Local runtime state / personal data (do NOT commit)
# history.jsonl = raw dictation transcripts (personal); overlay_pos.txt = pill position
# .autostart_initialized = one-time marker so autostart is registered only once
# stats.json = lifetime dictation stats rollup; config.toml.bak/.tmp = save_config artifacts
history.jsonl
overlay_pos.txt
.autostart_initialized
.guide_dismissed
stats.json
config.toml.bak
config.toml.tmp

# Secrets / local env
.env
*.env

# Model caches / large binaries
models/
*.bin
*.pt
*.onnx

# OS cruft
Thumbs.db
.DS_Store
